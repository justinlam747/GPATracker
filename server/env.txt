# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection
MONGODB_URI=mongodb+srv://kerotku_db_user:8uHfENOKd6j46Icn@gp8.lpqcn9l.mongodb.net/?retryWrites=true&w=majority&appName=GP8
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/gpa-tracker

# JWT Secrets (use different secrets for access and refresh tokens)
JWT_ACCESS_SECRET=your-super-secret-access-token-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-this-in-production
# Fallback for backward compatibility
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Origin
CLIENT_URL=http://localhost:3000

# Security Settings
# Rate limiting (requests per 15 minutes)
AUTH_RATE_LIMIT=20
GENERAL_RATE_LIMIT=100

# Password Policy
MIN_PASSWORD_LENGTH=8
MAX_PASSWORD_LENGTH=128

# Session Management
MAX_SESSIONS_PER_USER=5
ACCESS_TOKEN_EXPIRY=10m
REFRESH_TOKEN_EXPIRY=7d

# Account Lockout
MAX_FAILED_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_BASE=15m

# Email Verification
EMAIL_VERIFICATION_EXPIRY=24h

# Password Reset
PASSWORD_RESET_EXPIRY=15m

# Production Security (set to true in production)
HTTPS_ONLY=false
HSTS_MAX_AGE=31536000
